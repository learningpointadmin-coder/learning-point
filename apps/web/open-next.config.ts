import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Configures OpenNext to build the Next.js app for Cloudflare Workers.
// Run `npm run cf-build` to produce the `.open-next/` output directory.
export default defineCloudflareConfig();
